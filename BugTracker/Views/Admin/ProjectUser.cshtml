@model BugTracker.Models.AdminProjectUserAssignViewModel






<h2>ProjectUser - @Model.User.FirstName @Model.User.LastName</h2>

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.User.Id)

    @Html.AntiForgeryToken()
    @Html.ListBox("AddSelectedProjectUsers", Model.Users)
    @Html.ListBox("RemoveSelectedProjectUsers", Model.Users)

    <input type="submit" value="Save" class="btn btn-default" />
}

